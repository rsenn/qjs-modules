# iterator

Source: `lib/iterator.js` (pure JS) — default export: `Iterator`

A base class providing the synchronous iterator protocol and helper methods.

## Exports

| Export | Kind | Description |
| --- | --- | --- |
| `Iterator` | class | Iterator base (`next`, `[Symbol.iterator]`, helpers). **(default export)** |
| `IteratorPrototype` | const | Shortcut for `Iterator.prototype`. |
