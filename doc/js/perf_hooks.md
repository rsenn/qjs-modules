# perf_hooks

Source: `lib/perf_hooks.js` (pure JS) — default export: `performance`

Minimal Node `perf_hooks`-style timing.

## Exports

| Export | Args | Kind | Description |
| --- | --- | --- | --- |
| `performance` | — | const | Object with a `now()` method (and timing origin). **(default export)** |
| `now()` | 0 | function | High-resolution timestamp in milliseconds. |
