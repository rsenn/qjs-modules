# quickjs-modules

Some modules for QuickJS

## deep
  - find(object, (obj,key) => {})
  - get(object, pointer)
  - set(object, pointer, value)
  - unset(object, pointer)

## inspect
  - inspect(value[, options])

## mmap


## path

## pointer

## tree-walker

## xml


